interface:
  display_name: "以终为始"
  short_description: "从交付结果倒推真源、节点、验收和回滚"
  default_prompt: "Use $goal-first-control to turn this complex request into a goal contract, execution route, QA gates, and rollback-safe delivery."
policy:
  allow_implicit_invocation: true
